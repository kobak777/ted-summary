import { PageContainer } from "@/shared/ui/pageContainer";
import {TEDSummaryWidget} from "@/widgets/tedSummaryWidget";

export const HomePage = () => {
  return (
    <PageContainer>
      <TEDSummaryWidget />
    </PageContainer>
  );
};
